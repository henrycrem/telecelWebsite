'use client'

import React from "react";
import FirstSection from "./FirstSection/page";

const PricingPage = () => {
    return (
        React.createElement("div", null,
            React.createElement(FirstSection, null)
        )
    );
}

export default PricingPage;
